docker stop helloworld
docker rm helloworld
docker build -t hello-world /home/asyed/dockerdemo/cicd/
docker run -d --name hellowworld -p 80:80 -v //home/asyed/dockerdemo/scr/:/var/www/html hello-world
